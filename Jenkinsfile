pipeline {
    agent any
    stages {
        stage('Installing Pahse') {
            steps {
                echo 'Installing npm phase'
                sh 'npm install'
            }
        }
        stage('Lint Pahse') {
            steps {
                echo 'Installing npm phase'
                sh 'npm run lint'
            }
        }
    }
}